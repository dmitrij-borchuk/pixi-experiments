import { Scene } from 'phaser'

export class EditorScene extends Scene {
  static key = 'MaiEditorScene'
  constructor() {
    super('MaiEditorScene')
  }
  preload() {}
  create() {}
  update(time: number, delta: number) {}
}
